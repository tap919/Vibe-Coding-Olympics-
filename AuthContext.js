import React, { createContext, useContext, useState } from "react"; const AuthContext = createContext(); export const AuthProvider = ({ children }) => { const [isAuthenticated, setAuth] = useState(false); const [role, setRole] = useState(""); const login = (username, password) => { if (username === "admin" && password === "vibe2024") { setAuth(true); setRole("superadmin"); } }; return <AuthContext.Provider value={{ isAuthenticated, role, login }}>{children}</AuthContext.Provider>; }; export const useAuth = () => useContext(AuthContext);